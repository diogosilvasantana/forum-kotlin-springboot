server:
  port: 8090

spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/forum
    username: root
    password: aluracursos
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    jaxb:
      enabled=true

jwt:
  secret: secreto